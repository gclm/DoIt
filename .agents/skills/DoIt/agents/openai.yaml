interface:
  display_name: "DoIt"
  short_description: "Repo-specific patterns and workflows for DoIt"
  default_prompt: "Use the DoIt repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true